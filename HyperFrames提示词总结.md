# HyperFrames 生视频提示词指南总结

HyperFrames 是一个开源视频渲染框架，通过编写 HTML 并配合 AI Agent 来生成确定性、帧精确的视频。本指南总结了该项目的核心提示词体系。

---

## 一、提示词的两种形态

### 1. 冷启动（Cold Start）
从零开始描述你想要的视频，适用于绿色field工作。

```text
使用 /hyperframes，创建一个 10 秒的产品介绍视频，黑色背景上淡入标题，配合轻柔背景音乐。
```

关键参数：
- **时长**：「10 秒」、「3 个 3 秒的场景」
- **宽高比**：「16:9」、「9:16 竖屏」（默认 1920x1080）
- **情绪/风格**：「极简瑞士网格」、「高能量社交风格」
- **关键元素**：标题、字幕、音乐

### 2. 热启动（Warm Start）
提供已有内容（URL、文档、CSV、文字稿），让 Agent 将其合成为视频。

```text
查看这个 GitHub 仓库 https://github.com/heygen-com/hyperframes，使用 /hyperframes 解释其用途和架构。
```

热启动能产出更丰富、更有依据的视频，因为 Agent 是在描述具体内容而非编造文案。

---

## 二、一键提示词的六部分骨架

每个成功的提示词都遵循以下结构：

```text
[route]      /motion-graphics
[spec]       8秒 1920x1080 视频
[beats]      Beat 1 (0-4s): ...  Beat 2 (4-5s): ...  Beat 3 (5-8s): ...
[copy]       精确引用的屏幕文字
[technique]  使用 `apple-money-count` 注册块作为基础
[negatives]  无旁白，无图片或媒体文件
```

| 部分 | 作用 | 说明 |
|------|------|------|
| **Route（路由）** | 指定工作流 | 斜杠命令，加载正确的工作流和框架规则 |
| **Spec（规格）** | 时长和分辨率 | 默认 1920x1080 @ 30fps |
| **Beats（节拍）** | 带时间戳的场景 | 包含节奏指示（「保持在闪烁光标上」） |
| **Copy（文案）** | 精确引用的文字 | 用引号包裹，`/` 表示换行；未引用的文字会被改写 |
| **Technique（技术）** | 指定注册块 | 准确命名注册块以锁定技术手法 |
| **Negatives（负面）** | 明确排除项 | 「无旁白」≠「静音」；明确说明排除内容 |

### 每个节拍的五要素

```text
[element]   屏幕上有什么      一个巨大的数字 · 推文卡片 · "SHOWREEL"
[motion]    它做什么          里程表式计数 · 滑入 · 逐字母淡入
[layout]    它的位置          顶部居中 · 占据下半部分 · 右下
[style]     它的外观          深海军蓝，绿色强调 · 8位像素 · 细几何无衬线
[timing]    节拍内的时间      在1秒时 · 持续3秒，落点时缓出 · 错位0.1秒
```

**示例：**「一个巨大的数字以里程表滚动方式**计数**（动作），落在画面**中央**（位置），以绿色显示在深海军蓝**背景上**（风格），持续到**1秒**（时间）。」

---

## 三、工作流路由

### 核心工作流

| 命令 | 输入 → 输出 | 适用场景 |
|------|------------|----------|
| `/hyperframes` | 入口路由 | 首先使用此命令，进行意图识别和路由 |
| `/product-launch-video` | 网站URL/文案 → 发布视频 | SaaS推广、功能发布、产品演示 |
| `/faceless-explainer` | 任意文本 → 无旁白解释视频 | 文章、笔记、话题解释，所有视觉由AI生成 |
| `/pr-to-video` | GitHub PR → 代码变更视频 | 代码变更讲解 |
| `/motion-graphics` | 简短动态图形 | 计数动画、标志闪现、统计数字 |
| `/music-to-video` | 音乐 → 节拍同步视频 | 音乐视频、歌词视频、幻灯片 |
| `/general-video` | 通用视频 | 品牌视频、多场景蒙太奇、自定义合成 |

### 可选工作流

| 命令 | 说明 |
|------|------|
| `/embedded-captions` | 为现有视频添加字幕 |
| `/talking-head-recut` | 为采访视频添加动态图形覆盖 |
| `/slideshow` | 演示文稿/导航式deck |
| `/figma` | 从 Figma 文件导入 |

---

## 四、词汇映射表

### 1. 缓动词汇（Motion & Easing）

| 词汇 | Agent 使用 | 感觉 |
|------|-----------|------|
| `smooth` | `power2.out` | 自然减速 |
| `snappy` | `power4.out` | 快速果断 |
| `bouncy` | `back.out` | 过冲后稳定 |
| `springy` | `elastic.out` | 振荡到位 |
| `dramatic` | `expo.out` | 快速启动，长距离滑行 |
| `dreamy` | `sine.inOut` | 缓慢对称 |

**节奏速记：** 快速(0.2s) = 活力，中速(0.4s) = 专业，慢速(0.6s) = 奢华，极慢(1-2s) = 电影感

### 2. 镜头语言（Camera Language）

| 词汇 | Agent 构建 |
|------|-----------|
| `slow push-in` | 场景缩放 4-8%，缓入 |
| `pull back` | 缩放减小，配合元素进入 |
| `pan across` | 场景层水平平移 |
| `drone orbit` | 持续相机轨道（Three.js 场景） |
| `crane down/up` | 垂直相机漂移 + 视点偏移 |
| `whip to` | 快速模糊滑入下一帧 |
| `parallax` | 多层次不同速率平移产生深度 |

### 3. 深度语言（Depth Language）

| 词汇 | 效果 |
|------|------|
| `near-lens` / `foreground bokeh` | 大而模糊的元素靠近相机 |
| `depth planes` | 2-3层以不同速度移动 |
| `out-of-focus background` | 模糊缓慢移动的背景层 |
| `shallow depth of field` | 主体清晰，其他均模糊 |

### 4. 节奏语言（Pacing Language）

| 词汇 | 效果 |
|------|------|
| `punchy cuts` | 1.5-4秒/创意，硬切，重叠进入 |
| `cinematic holds` | 较长节拍 + 环境闲置运动 |
| `beat-synced` | 按音乐节拍网格剪辑和强调 |
| `breathing room` | 下一节拍开始前的停顿 |
| `ambient idle` | 1-2%呼吸缩放 + 缓慢漂移 |

### 5. 字幕音调（Caption Tones）

| 风格 | 字体 | 动画 | 字号范围 |
|------|------|------|---------|
| **Hype** | 粗体字 | 弹性缩放 | 72-96px |
| **Corporate** | 简洁无衬线 | 淡入+滑动 | 56-72px |
| **Tutorial** | 等宽字体 | 打字机 | 48-64px |
| **Storytelling** | 衬线字体 | 慢速淡入 | 44-56px |
| **Social** | 圆润活泼 | 弹跳 | 56-80px |

### 6. 转场（Transitions）

| 能量级别 | Shader 首选 | CSS 首选 |
|---------|------------|----------|
| **Calm** | `cross-warp-morph` | `transitions-blur` |
| **Medium** | `whip-pan` | `transitions-push` |
| **High** | `ridged-burn`, `glitch` | `transitions-scale` |

### 7. 音频反应动画

| 音频频段 | 映射到 | 视觉效果 |
|---------|--------|---------|
| **Bass（低音）** | `scale` | 节拍脉冲 |
| **Treble（高音）** | `glow` | 微光强度 |
| **Amplitude（振幅）** | `opacity` | 呼吸效果 |
| **Mids（中音）** | `shape` | 形态变化 |

### 8. 标记高亮（Marker Highlights）

| 模式 | 效果 | 适用场景 |
|------|------|---------|
| `highlight` | 标记笔扫过 | 关键短语 |
| `circle` | 手绘椭圆 | 单个词汇 |
| `burst` | 放射线 | 精彩瞬间 |
| `scribble` | 混乱涂鸦 | 划掉内容 |
| `sketchout` | 矩形轮廓 | 标注说明 |

### 9. TTS 语音推荐

| 内容类型 | 推荐语音 |
|---------|---------|
| 产品演示 | `af_heart`, `af_nova` |
| 教程 | `am_adam`, `bf_emma` |
| 营销 | `af_sky`, `am_michael` |

### 10. 渲染质量

| 质量 | 用途 |
|------|------|
| `draft` | 快速迭代 |
| `standard` | 审阅和反馈 |
| `high` | 最终交付 |

---

## 五、运动的八条语法规则

### 规则 1：永不停下
每个「保持」都要有环境闲置运动 — 1-2% 呼吸缩放、缓慢漂移、微光。
- ❌ 「保持静止不动」
- ✅ 「进入轻柔的环境闲置状态」

### 规则 2：相机是演员
为每个场景提供一个连续的相机移动 — 4-8% 推入、缓慢轨道、视差分层。
- ❌ 「相机从不移动」
- ✅ 「相机沿轨道连续移动，ease: none」

### 规则 3：重叠动作
没有两个元素在同一时间开始或结束。入口以不规则偏移错位；下一个元素在上一个仍在稳定时就开始。
- ❌ 「所有12个区域同时点亮」
- ✅ 「从 us-east-1 向外扩散，每个区域按距离顺序到达，间隔0.09秒」

### 规则 4：复合属性只讲一个故事
每个动画至少改变一个属性。组合它们只有在表达同一含义时才有意义。
- ❌ 「同时上升、旋转、缩放和模糊」
- ✅ 「上升并淡入」

### 规则 5：过冲和跟随
过冲是关于质量的声明。阴影和尾随面板比主体晚一帧到位。仅应用于**变换**，不适用于数字。
- ❌ 「线性，无过冲」
- ✅ 「back.out(5) 摆动经过静止位置并回归」

### 规则 6：深度平面
每个平面以与其距离匹配的速率移动。最强的提示是**遮挡**。
- ❌ 「所有平面以相同速率漂移」
- ✅ 「远处行以房间速率的1/5移动，近处柱以6倍速率扫过」

### 规则 7：节奏匹配类型
展示风格剪辑为 1.5-4 秒/创意；一个 2 秒创意的 8 秒版本无论如何动画都会感觉慢。

### 规则 8：手工缺陷保持可复现
想要定格动画/手工感觉？使用 PRNG 种子并以两帧为单位量化。
- ❌ `Math.random()` 会破坏确定性
- ✅ 「mulberry32，种子42，每个位置保持两帧」

---

## 六、代码动画提示词

### 选择正确的块

| 你想展示... | 使用此块 | 长度 |
|------------|---------|------|
| 逐字符编写代码 | `code-typing` | 5s |
| 编辑（前后对比） | `code-diff` | 6s |
| 某一行作为重点 | `code-highlight` | 5s |
| 在长文件中滚动定位 | `code-scroll` | 6s |
| 代码变形 | `code-morph` | 7s |

### 选择终端/编辑器主题

- **macOS Terminal 配置文件**：Ocean、Pro、Homebrew、Red Sands 等
- **VS Code 主题**：Monokai、Dark Modern、Solarized Light 等

**最佳实践：** 将代码作为字面代码块引用（引号包裹），因为未引用的代码会被改写。

---

## 七、提示词的常见错误修正

| 错误模式 | 修正方案 |
|---------|---------|
| 「保持静止」→ 渲染冻结帧 | 「进入轻柔的环境闲置状态（呼吸缩放，缓慢漂移）」 |
| 时长大于描述内容 | 「12秒视频。[节拍]...然后在完成的图表上保持约5秒」 |
| 两个事件「在4秒时」同时发生 | 「计数器在4.2秒前完全淡出；在4.2秒时READY打入」 |
| 「无外部资源」含义模糊 | 「无图片或媒体文件」 |
| 散文式文案 | 直接引用：「标语：『快速交付。』」 |
| 「从零数到$4.2M」格式不匹配 | 「计数上升至$4.2M」 |
| 「60秒」而非「~60秒」 | 使用「~60秒」，因为实际时长由旁白决定 |

---

## 八、完整提示词示例

### 产品发布视频
```text
/product-launch-video 为 https://linear.app 创建一个45秒1920x1080发布视频。
充满活力但简约，使用网站自身的调色板和截图。
结构：问题钩子、3个功能节拍（UI截图+一行字幕）、结尾卡片（logo + "免费试用"）。
女性TTS语音，自信语调，-18dB电子背景音乐。
```

### 统计数字计数动画
```text
/motion-graphics 6秒1920x1080视频，深海军蓝背景。
Beat 1 (0-1s): 标签"ARR"在顶部中央淡入。
Beat 2 (1-4s): 巨大数字以里程表式滚动计数至$4.2M，落点时缓出。
Beat 3 (4-6s): "+312% YoY"以绿色在下方打入，然后进入轻柔的环境闲置状态。
使用 `apple-money-count` 注册块作为基础。无旁白。
```

### 无旁白解释视频
```text
/faceless-explainer 将此转换为约60秒的1080x1920竖屏解释视频：[粘贴你的文本]。
每个场景一个创意，大字体，品牌色 #FF5533 配深黑背景。
男性TTS语音，平静语调。嵌入式字幕，关键词用品牌色高亮。
```

---

## 九、推荐工作流

1. `npx hyperframes init my-video` — 搭建项目
2. 在 AI 编码编辑器中打开项目
3. 使用 `/hyperframes` 和提示词开始
4. `npx hyperframes preview` — 在浏览器中预览
5. 使用小而精确的提示词迭代
6. `npx hyperframes lint && npx hyperframes check` — 验证
7. `npx hyperframes render --output final.mp4` — 渲染输出

---

## 十、框架核心术语

| 文件中 | 含义 |
|--------|------|
| `class="clip"` | 标记定时区域。框架管理可见性 — 永不要通过动画 opacity/display 隐藏 |
| `data-start` / `data-duration` / `data-track-index` | 剪辑开始时间、持续时长、所在轨道。同一轨道的两个剪辑时间不得重叠 |
| `window.__timelines["<id>"]` | 每个 GSAP 合成作品在此注册一个暂停时间线。渲染器逐帧搜索 |
| `<video muted>` + `<audio>` | 视频元素渲染为静音，声音由兄弟 audio 元素承载，混音器独立控制 |

---

*本总结基于 HyperFrames 官方文档整理，涵盖提示词体系的核心要点。*
